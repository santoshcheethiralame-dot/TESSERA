[package]
name = "consensus"
version.workspace = true
edition.workspace = true

[dependencies]
sim = { path = "../sim" }

[dev-dependencies]
sim = { path = "../sim" }
lincheck = { path = "../lincheck" }
